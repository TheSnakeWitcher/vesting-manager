{
  "name": "@thesnakewitcher/vesting-manager",
  "author": "Alex Virelles <thesnakewitcher@gmail.com>",
  "version": "0.1.1",
  "description": "A decentralized vesting service",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "pnpm hardhat compile",
    "build": "tsc",
    "prepublishOnly": "pnpm compile && pnpm build",
    "release:create": "VERSION=$(git describe main --tags) ; git push origin $VERSION ; gh release create $VERSION --generate-notes",
    "release:publish": "pnpm publish --no-git-checks --access public" ,
    "release": "pnpm release:create && pnpm release:publish"
  },
  "license": "BUSL",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@solarity/hardhat-gobind": "^1.3.0",
    "hardhat": "^2.24.0",
    "hardhat-deploy": "^1.0.4",
    "hardhat-inspect": "^0.1.15",
    "viem": "^2.30.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@thesnakewitcher/contracts-fees": "^0.1.1",
    "ethers": "^6.15.0"
  }
}
